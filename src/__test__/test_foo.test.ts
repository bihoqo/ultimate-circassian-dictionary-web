import { describe, it, expect } from "@jest/globals";

describe("foo", () => {
  it("foo", () => {
    const input = "foo";
    const expectedOutput = "foo";
    expect(input).toBe(expectedOutput);
  });
});
